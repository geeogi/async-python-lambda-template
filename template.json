{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Resources": {
    "PythonLambdaS3Bucket": {
      "Type": "AWS::S3::Bucket",
      "DeletionPolicy": "Delete"
    },
    "PythonLambdaRole": {
      "Type": "AWS::IAM::Role",
      "Properties": {
        "AssumeRolePolicyDocument": {
          "Statement": [
            {
              "Effect": "Allow",
              "Principal": {
                "Service": ["lambda.amazonaws.com"]
              },
              "Action": ["sts:AssumeRole"]
            }
          ]
        },
        "Policies": [
          {
            "PolicyName": "PythonLambdaRolePolicy",
            "PolicyDocument": {
              "Statement": [
                {
                  "Effect": "Allow",
                  "Action": "s3:*",
                  "Resource": {
                    "Fn::Join": ["", ["arn:aws:s3:::", { "Ref": "PythonLambdaS3Bucket" }, "/*"]]
                  }
                },
                {
                  "Effect": "Allow",
                  "Action": ["logs:CreateLogGroup", "logs:CreateLogStream", "logs:PutLogEvents"],
                  "Resource": "*"
                }
              ]
            }
          }
        ]
      }
    },
    "PythonLambda": {
      "Type": "AWS::Lambda::Function",
      "Properties": {
        "Handler": "index.handler",
        "Code": {
          "ZipFile": { "Fn::Join": ["\n", ["def handler(event, context):", "pass"]] }
        },
        "Environment": {
          "Variables": { "BUCKET_NAME": { "Ref": "PythonLambdaS3Bucket" } }
        },
        "Runtime": "python3.7",
        "Timeout": 5,
        "Role": {
          "Fn::GetAtt": ["PythonLambdaRole", "Arn"]
        }
      }
    }
  }
}
